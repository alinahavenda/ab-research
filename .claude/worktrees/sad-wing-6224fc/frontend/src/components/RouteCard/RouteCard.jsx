// TODO: implement — used by both versions (different detail levels)
// Version A: full details (carrier, rating, routeNumber, busType)
// Version B: minimal (time, price, duration, stars only)
export default function RouteCard({ route, variant, onSelect }) {
  return <div>RouteCard — TODO</div>;
}
